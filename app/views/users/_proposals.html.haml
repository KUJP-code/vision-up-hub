-# locals: (proposals:)

- if proposals.any?
  %h3.text-center Proposals
  %table.w-full.text-center
    %thead
      %tr
        %th Lesson
        %th Status
        %th Submitted
        %th Comments
        %th
    %tbody
      - proposals.each do |proposal|
        %tr
          %td.p-3= proposal.status.titleize
          %td.p-3= proposal.created_at.strftime('%d/%m/%Y')
          %td.p-3= proposal.internal_notes
          %td.p-3
            = link_to 'Edit',
                      edit_lesson_path(proposal),
                      class: 'btn btn-info'
