%main.flex.flex-col.gap-3.p-3
  = link_to 'Create course',
            new_course_path,
            class: 'btn btn-primary'

  %h2.text-4xl Courses
  = render 'courses/course_table', courses: @courses
