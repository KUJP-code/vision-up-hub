= render 'shared/subtitle', text: "Creating #{@tutorial.class.name.titleize}"

%main
  = render "tutorials/forms/#{@type.downcase}_form",
           tutorial: @tutorial,
           categories: @categories
