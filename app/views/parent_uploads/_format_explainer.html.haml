%section.w-full.text-center.flex.flex-col.gap-3
  %h3 CSVs should be formatted as follows:
  %table.w-full
    %thead
      %tr
        - Parent::CSV_HEADERS.each do |header|
          :ruby
            classes = 'thead bg-secondary-50'
            classes = "#{classes} thead-s" if header == Parent::CSV_HEADERS.first
            classes = "#{classes} thead-e" if header == Parent::CSV_HEADERS.last

          %th{ class: classes }= header
    %tbody
      %tr
        %td 陽葵
        %td example@gmail.com
        %td akgsdgh3242%($
      %tr
        %td ...
        %td ...
        %td ...
      %tr
        %td Name in Japanese or English
        %td Parent's email
        %td Parent's password
