-# locals: (f:, lesson:)

.form-group
  = f.label :subtype
  = f.select :subtype,
             Exercise.subtypes.keys.map { |k| [k.titleize, k] },
             {}

.form-group
  = f.label :warning
  = f.text_field :warning

- Exercise::LISTABLE_ATTRIBUTES.each do |attr|
  = render('lessons/shared/list_fields', attr:, f:, lesson:)
