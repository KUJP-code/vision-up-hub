-# locals: (lesson:)

- if lesson.guide.present?
  = link_to file_path(lesson.guide.blob),
            target: '_blank',
            rel: 'noopener',
            class: 'guide_link h-fit' do
    = image_tag lesson.guide.preview(:thumb),
                class: 'guide_image w-full'
    %h3.rounded-lg.text-white.bg-ku-orange.text-center.p-2.font-semibold
      Download Guide

- else
  %h3.guide_link Guide is being generated, refresh the page in ~10 seconds
