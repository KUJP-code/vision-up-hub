-# locals: (lesson:)

%section{ class: 'w-full md:w-1/3' }
  = link_to file_path(id: lesson.guide.blob.id),
            target: '_blank',
            rel: 'noopener',
            class: 'btn-resource' do
    %span{ class: 'basis-5/6 truncate' } Guide
    = render 'shared/svgs/download_icon',
             classes: 'fill-secondary'
