-# locals: (images:)

%section.flex.flex-wrap.justify-center.items-center.gap-3
  - images.each do |image|
    = link_to file_path(image.blob.id),
              target: '_blank',
              rel: 'noopener',
              class: 'w-1/3 md:w-1/12' do
      = image_tag image, class: ' border-2 border-secondary rounded'
